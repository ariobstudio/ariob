import header from '../component/header.js';

const activity = `
<div id="activity" class="page hold center">
    ${header('Activity')}
    
    <div class="center gap leak">
      <p> Activity - In Development</p>
    </div>
  </div>
`

export default activity