import header from '../component/header.js';

const settings = `
<div id="settings" class="page hold center">
    ${header('Settings')}
    
    <div class="center gap leak">
      <p>Settings - In Development</p>
    </div>
  </div>
`

export default settings 