{
  "name": "ariob",
  "version": "1.0.0",
  "description": "Cross platform Decentralized Platform",
  "main": "index.js",
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "platforms/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "dev:andromeda": "turbo run dev --filter=andromeda",
    "build": "turbo run build",
    "build:andromeda": "turbo run build --filter=andromeda",
    "build:homepage": "turbo run build --filter=homepage",
    "build:ariob": "turbo run build --filter=ariob",
    "build:brana": "turbo run build --filter=brana"
  },
  "packageManager": "pnpm@8.15.4",
  "devDependencies": {
    "turbo": "^2.5.2"
  }
}
